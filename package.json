{
  "name": "pixel-port-site",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "build": "node src/build.mjs",
    "test": "node --test tests/*.test.mjs"
  },
  "dependencies": {
    "gray-matter": "^4.0.3",
    "marked": "^15.0.7"
  }
}
